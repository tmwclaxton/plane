name: Production Deploy

on:
  workflow_dispatch:
  push:
    branches: [lgs/bulk-due-dates]

concurrency:
  group: plane-production-deploy
  cancel-in-progress: false

env:
  DEPLOY_HOST: admin.grantgunner.org
  DEPLOY_USER: gunnergrant
  DEPLOY_DIR: /home/gunnergrant/plane-selfhost/plane-app
  PLANE_SRC_DIR: /home/gunnergrant/plane-selfhost/plane-src

jobs:
  deploy:
    name: Rebuild web image and deploy to plane.canvassr.org
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Install cloudflared
        run: |
          curl -fsSL --output /tmp/cloudflared.deb \
            https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i /tmp/cloudflared.deb

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          cat >> ~/.ssh/config << EOF
          Host ${{ env.DEPLOY_HOST }}
            User ${{ env.DEPLOY_USER }}
            IdentityFile ~/.ssh/deploy_key
            BatchMode yes
            ConnectTimeout 30
            ServerAliveInterval 30
            ServerAliveCountMax 120
            StrictHostKeyChecking accept-new
            ProxyCommand cloudflared access ssh --hostname %h
          EOF

      - name: Sync source, rebuild frontend, deploy overlay
        env:
          CF_ACCESS_CLIENT_ID: ${{ secrets.CF_ACCESS_CLIENT_ID }}
          CF_ACCESS_CLIENT_SECRET: ${{ secrets.CF_ACCESS_CLIENT_SECRET }}
        run: |
          ssh ${{ env.DEPLOY_HOST }} "mkdir -p ${{ env.DEPLOY_DIR }}/og ${{ env.DEPLOY_DIR }}/public-frontend ${{ env.PLANE_SRC_DIR }}"
          rsync -a --delete \
            --exclude '.git' \
            --exclude 'node_modules' \
            --exclude '.turbo' \
            --exclude '.next' \
            --exclude '.react-router' \
            --exclude 'build' \
            --exclude 'dist' \
            --exclude 'out' \
            ./ ${{ env.DEPLOY_HOST }}:${{ env.PLANE_SRC_DIR }}/
          rsync -a --delete \
            --exclude '.git' \
            deploy/og/ ${{ env.DEPLOY_HOST }}:${{ env.DEPLOY_DIR }}/og/
          rsync -a --delete \
            --exclude '.git' \
            deploy/public-frontend/ ${{ env.DEPLOY_HOST }}:${{ env.DEPLOY_DIR }}/public-frontend/
          scp deploy/prod-deploy.sh ${{ env.DEPLOY_HOST }}:${{ env.DEPLOY_DIR }}/prod-deploy.sh
          ssh ${{ env.DEPLOY_HOST }} "chmod +x ${{ env.DEPLOY_DIR }}/prod-deploy.sh && PLANE_SRC_DIR=${{ env.PLANE_SRC_DIR }} ${{ env.DEPLOY_DIR }}/prod-deploy.sh"
